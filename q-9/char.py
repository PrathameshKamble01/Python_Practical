'''
Title:
    Print Each Character in a String

Prerequisites:
    Basic understanding of Python programming syntax, including input/output operations and loops.

Objectives:

    Prompt the user to input a string.
    Iterate through each character in the string.
    Print each character on a new line.

Algorithm:

    Begin by prompting the user to enter a string.
    Read the input string from the user.
    Use a loop (e.g., for loop) to iterate through each character in the string.
    Inside the loop, print each character on a new line.
    End.
'''

input_string = input("Enter a string: ")

for char in input_string:
    print(char)
